services:
  mirofish:
    build: .
    # image: ghcr.io/666ghj/mirofish:latest  # 원본 중국어 이미지 (비활성)
    container_name: mirofish
    env_file:
      - .env
    ports:
      - "3000:3000"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
